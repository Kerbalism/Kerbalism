// Disable DBS if present. DBS contains code internally to detect Kerbalism and will
// disable itself if it does, but that might be removed one day and doing it via confg
// on our side is the preferrable solution

@DYNAMICBATTERYSTORAGE:NEEDS[DynamicBatteryStorage]:FOR[Kerbalism]
{
	Enabled = false
}

