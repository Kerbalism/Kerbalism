// Principia does some very cruel things to the tracking station,
// the stock orbit lines don't exist any more, so tweaking them is impossible

@KERBALISM_SETTINGS:NEEDS[Principia]
{
  %EnableOrbitLineTweaks = false
}
